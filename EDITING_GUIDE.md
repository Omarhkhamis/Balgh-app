# 📝 دليل التعديل المبسط - منصة بَلِّغ

هذا الدليل موجه لغير المتخصصين لتسهيل التعديل على المنصة بعد رفعها على GitHub.

---

## 🎯 الطرق المتاحة للتعديل

### **الطريقة 1: GitHub Web Editor (الأسهل والأسرع)** ⭐

#### الخطوات:
1. افتح مستودع المشروع على GitHub
2. اضغط على زر `.` (نقطة) على لوحة المفاتيح
3. سيفتح محرر VS Code في المتصفح مباشرة
4. عدّل الملفات المطلوبة
5. اضغط على أيقونة Source Control (الفرع الثالث في القائمة اليسرى)
6. اكتب رسالة توضيحية للتغييرات
7. اضغط `Commit & Push`

**مناسب لـ:** تعديل النصوص، الترجمات، الألوان، الروابط

---

### **الطريقة 2: GitHub Codespaces (للتعديلات الأكبر)** ⭐⭐

#### الخطوات:
1. من صفحة المستودع على GitHub
2. اضغط على زر `Code` (الأخضر)
3. اختر تبويب `Codespaces`
4. اضغط `Create codespace on main`
5. انتظر تحميل البيئة (دقيقة واحدة تقريباً)
6. في Terminal الموجود أسفل الشاشة، اكتب:
   ```bash
   npm install
   npm run dev
   ```
7. سيفتح رابط معاينة المشروع
8. عدّل الملفات وشاهد التغييرات مباشرة
9. عند الانتهاء، ارفع التغييرات كما في الطريقة 1

**مناسب لـ:** إضافة صفحات جديدة، تعديل التصميم، تجربة التغييرات قبل النشر

---

### **الطريقة 3: التعديل المباشر على GitHub (للتعديلات البسيطة جداً)**

#### الخطوات:
1. افتح الملف المطلوب على GitHub
2. اضغط على أيقونة القلم (Edit) في أعلى يمين الملف
3. عدّل المحتوى
4. اضغط `Commit changes` في الأعلى
5. اكتب رسالة توضيحية واضغط `Commit changes`

**مناسب لـ:** تعديل نص واحد، تصحيح خطأ إملائي

---

## 📂 دليل الملفات الشائعة للتعديل

### **1. تعديل النصوص والترجمات** 📝

#### الملفات:
- **العربية**: `messages/ar.json`
- **الإنجليزية**: `messages/en.json`

#### مثال على التعديل:
```json
{
  "hero": {
    "title": "النص الجديد هنا",
    "description": "الوصف الجديد"
  }
}
```

**ملاحظة:** احذر من حذف الأقواس `{}` أو الفواصل `,`

---

### **2. تعديل الصور والشعارات** 🖼️

#### المجلد: `public/`

#### لتغيير الشعار:
1. ضع الصورة الجديدة في مجلد `public/`
2. سمّها `logo.jpg` (أو غيّر الاسم في الكود)

#### لإضافة صور جديدة:
1. ضع الصورة في `public/images/`
2. استخدمها في الكود بهذا الشكل:
   ```tsx
   <img src="/images/اسم-الصورة.jpg" alt="وصف" />
   ```

---

### **3. تعديل الألوان والتصميم** 🎨

#### الملف: `src/app/[locale]/globals.css`

#### الألوان الرئيسية:
```css
:root {
  --primary-green: #10b981;    /* اللون الأخضر الأساسي */
  --dark-bg: #0f172a;          /* خلفية داكنة */
  --light-text: #f1f5f9;       /* نص فاتح */
}
```

#### لتغيير لون معين:
ابحث عن اسم اللون في الملف وغيّر القيمة (مثل `#10b981`)

**أداة مساعدة:** استخدم [Color Picker](https://htmlcolorcodes.com/) لاختيار الألوان

---

### **4. تعديل الصفحة الرئيسية** 🏠

#### الملف: `src/app/[locale]/page.tsx`

هذا الملف يحتوي على:
- قسم البطل (Hero Section)
- نموذج التحليل
- الأقسام الأخرى

**نصيحة:** ابحث عن النص الذي تريد تعديله في الملف واستبدله

---

### **5. إضافة صفحة جديدة** ➕

#### الخطوات:
1. أنشئ مجلد جديد في `src/app/[locale]/اسم-الصفحة/`
2. أنشئ ملف `page.tsx` داخله
3. انسخ هذا القالب:

```tsx
export default function NewPage() {
  return (
    <div className="min-h-screen bg-slate-900 text-white p-8">
      <h1 className="text-4xl font-bold mb-4">عنوان الصفحة</h1>
      <p>محتوى الصفحة هنا</p>
    </div>
  );
}
```

4. افتح الصفحة من: `https://موقعك.com/ar/اسم-الصفحة`

---

### **6. تعديل القائمة الرئيسية (Navbar)** 🧭

#### الملف: `src/components/Navbar.tsx`

#### لإضافة رابط جديد:
```tsx
<Link href="/ar/صفحة-جديدة" className="...">
  اسم الرابط
</Link>
```

---

### **7. تعديل معلومات الاتصال** 📞

#### الملف: `src/components/Footer.tsx`

ابحث عن:
- البريد الإلكتروني
- روابط وسائل التواصل
- معلومات الاتصال

وعدّلها حسب الحاجة.

---

## 🔧 تعديلات شائعة

### **تغيير عنوان الموقع في المتصفح**

#### الملف: `src/app/[locale]/layout.tsx`

```tsx
export const metadata = {
  title: "العنوان الجديد",
  description: "الوصف الجديد",
};
```

---

### **تعديل رابط Google Sheets**

#### الملف: `.env.local`

```env
SPREADSHEET_ID=رقم_الشيت_الجديد
```

**كيفية الحصول على الرقم:**
من رابط Google Sheet: `https://docs.google.com/spreadsheets/d/[هذا_هو_الرقم]/edit`

---

### **تعديل نصوص التقارير القانونية**

#### الملف: `src/lib/legalReportGenerator.ts`

ابحث عن اسم الدولة وعدّل النصوص المرتبطة بها.

---

## ⚠️ تحذيرات مهمة

### **لا تعدّل هذه الملفات إلا إذا كنت متأكداً:**
- ❌ `package.json` (إعدادات المشروع)
- ❌ `next.config.ts` (إعدادات Next.js)
- ❌ `tsconfig.json` (إعدادات TypeScript)
- ❌ ملفات `.env` (لا ترفعها على GitHub أبداً!)

### **قبل أي تعديل:**
1. ✅ اعمل نسخة احتياطية (Branch جديد)
2. ✅ جرّب التعديل في Codespaces أولاً
3. ✅ اقرأ الكود المحيط بالجزء الذي تريد تعديله

---

## 🆘 حل المشاكل الشائعة

### **المشكلة: الموقع لا يعمل بعد التعديل**

#### الحل:
1. تأكد من عدم وجود أخطاء في الكود
2. في Codespaces، افتح Terminal واكتب:
   ```bash
   npm run build
   ```
3. إذا ظهرت أخطاء، اقرأها بعناية (عادة تخبرك بمكان الخطأ)

---

### **المشكلة: التعديلات لا تظهر على الموقع المباشر**

#### الحل:
1. تأكد من رفع التعديلات (Commit & Push)
2. إذا كنت تستخدم Vercel/Netlify، انتظر إعادة النشر (1-3 دقائق)
3. امسح ذاكرة التخزين المؤقت (Cache) في المتصفح: `Ctrl+Shift+R`

---

### **المشكلة: نسيت كلمة المرور أو المفاتيح**

#### الحل:
- مفاتيح API موجودة في `.env.local` (لا تُرفع على GitHub)
- احتفظ بنسخة آمنة منها في مكان خارجي

---

## 📚 موارد مفيدة

### **تعلم الأساسيات:**
- [دورة GitHub للمبتدئين](https://skills.github.com/)
- [أساسيات JSON](https://www.json.org/json-ar.html)
- [مقدمة في Tailwind CSS](https://tailwindcss.com/docs)

### **أدوات مساعدة:**
- [Color Picker](https://htmlcolorcodes.com/) - اختيار الألوان
- [JSON Validator](https://jsonlint.com/) - التحقق من صحة JSON
- [Image Compressor](https://tinypng.com/) - ضغط الصور

---

## 💡 نصائح للنجاح

1. **ابدأ بتعديلات صغيرة**: لا تغيّر أشياء كثيرة دفعة واحدة
2. **استخدم Codespaces للتجربة**: شاهد التغييرات قبل النشر
3. **اكتب رسائل Commit واضحة**: مثل "تعديل نص الصفحة الرئيسية"
4. **احتفظ بنسخ احتياطية**: أنشئ Branch جديد قبل التعديلات الكبيرة
5. **اطلب المساعدة**: لا تتردد في السؤال إذا واجهت مشكلة

---

## 🎓 خطوات البداية الموصى بها

### **للمبتدئين تماماً:**
1. ابدأ بتعديل ملف `messages/ar.json` (تغيير نص بسيط)
2. استخدم GitHub Web Editor (اضغط `.`)
3. شاهد التغيير على الموقع المباشر

### **للمستخدمين المتوسطين:**
1. استخدم GitHub Codespaces
2. عدّل الألوان في `globals.css`
3. أضف صورة جديدة في `public/images/`
4. شاهد التغييرات في المعاينة المباشرة

### **للمستخدمين المتقدمين:**
1. أنشئ صفحة جديدة
2. عدّل مكونات React في `src/components/`
3. أضف ميزات جديدة

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. ارجع لهذا الدليل
2. ابحث عن الخطأ في Google
3. اسأل في مجتمعات البرمجة العربية
4. تواصل مع فريق الدعم

---

**صُنع بـ ❤️ لتسهيل التعديل على منصة بَلِّغ**

آخر تحديث: نوفمبر 2025
